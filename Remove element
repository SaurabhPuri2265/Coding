class Solution {
    public int removeElement(int[] arr, int target) {
        int l=arr.length,ind=0;
	    	for(int i=0;i<arr.length;i++)
	    	{
	    		if(arr[i]!=target)
	    		{
	    		    arr[ind]=arr[i];
                    ind++;
	    		}
	    			
	    	}
        return ind;
    }
}
